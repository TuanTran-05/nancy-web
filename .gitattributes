# core.autocrlf=true đổi LF thành CRLF khi checkout trên Windows, nên cùng một
# commit lại cho ra nội dung khác nhau giữa các bản checkout. Hai tệp dưới đây
# được test so sánh nguyên văn từng byte, nên phải ghim LF để kết quả không phụ
# thuộc vào cấu hình máy.
robots.txt text eol=lf
sitemap.xml text eol=lf
